<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guet.dao.LateTrainMapper">
  <resultMap id="BaseResultMap" type="com.guet.entity.LateTrain">
    <id column="lt_id" jdbcType="BIGINT" property="ltId" />
    <result column="new_time" jdbcType="TIMESTAMP" property="newTime" />
    <result column="type" jdbcType="TINYINT" property="type" />
    <result column="tt_id" jdbcType="BIGINT" property="ttId"/>
  </resultMap>
  <sql id="Base_Column_List">
    lt_id, tt_id, new_time, type
  </sql>

  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select 
    *
    from late_train
    where lt_id = #{ltId,jdbcType=BIGINT}
  </select>

  <select id="selectByttId" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select
    *
    from late_train
    where tt_id = #{ttId,jdbcType=BIGINT}
  </select>

  <select id="selectAll" resultMap="BaseResultMap">
    select
    *
    from late_train lt join train_ticket tt on lt.tt_id=tt.tt_id
  </select>

  <select id="selectAllPage" resultMap="BaseResultMap">
    select
    *
    from late_train lt join train_ticket tt on lt.tt_id=tt.tt_id
    <if test="keyWord != null">
      where #{column} = #{keyWord}
    </if>
    limit #{start},#{limit}
  </select>

  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from late_train
    where lt_id = #{ltId,jdbcType=BIGINT}
  </delete>

  <insert id="insert" parameterType="com.guet.entity.LateTrain">
    <selectKey keyProperty="ltId" order="AFTER" resultType="java.lang.Long">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into late_train (tt_id, new_time, type)
    values (#{trainTicket.ttId,jdbcType=BIGINT}, #{newTime,jdbcType=TIMESTAMP},#{type,jdbcType=TINYINT})
  </insert>

  <insert id="insertSelective" parameterType="com.guet.entity.LateTrain">
    <selectKey keyProperty="ltId" order="AFTER" resultType="java.lang.Long">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into late_train
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="trainTicket.ttId != null">
        tt_id,
      </if>
      <if test="newTime != null">
        new_time,
      </if>
      <if test="type != null">
        type,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="trainTicket.ttId != null">
        #{trainTicket.ttId,jdbcType=BIGINT},
      </if>
      <if test="newTime != null">
        #{newTime,jdbcType=TIMESTAMP},
      </if>
      <if test="type != null">
        #{type,jdbcType=TINYINT},
      </if>
    </trim>
  </insert>

  <update id="updateByPrimaryKeySelective" parameterType="com.guet.entity.LateTrain">
    update late_train
    <set>
      <if test="trainTicket.ttId != null">
        tt_id = #{trainTicket.ttId,jdbcType=BIGINT},
      </if>
      <if test="newTime != null">
        new_time = #{newTime,jdbcType=TIMESTAMP},
      </if>
      <if test="type != null">
        #{type,jdbcType=TINYINT},
      </if>
    </set>
    where lt_id = #{ltId,jdbcType=BIGINT}
  </update>

  <update id="updateByPrimaryKey" parameterType="com.guet.entity.LateTrain">
    update late_train
    set tt_id = #{trainTicket.ttId,jdbcType=BIGINT},
      new_time = #{newTime,jdbcType=TIMESTAMP},
      type = #{type,jdbcType=TINYINT}
    where lt_id = #{ltId,jdbcType=BIGINT}
  </update>

</mapper>